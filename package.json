{
  "name": "ipas-study",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build-static.mjs",
    "deploy:dry-run": "npm run build && wrangler deploy --dry-run",
    "deploy": "npm run build && wrangler deploy --strict"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8",
    "wrangler": "4.132.0"
  }
}
